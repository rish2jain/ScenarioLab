# AGENTS.md

**Full project guidance is in [`CLAUDE.md`](./CLAUDE.md).** Use that file for the project overview, monorepo architecture, backend module table, LLM provider list (including CLI providers and timeouts), frontend structure, simulation wizard, API wiring, configuration table, **Common Commands** (including `scenariolab-sim` CLI), and key design decisions.

**Codex (Codex.ai/code):** Follow the same guidance as `CLAUDE.md`. This stub exists so tools that look for `AGENTS.md` at the repo root can find the guide without maintaining a second full copy (avoids drift).
